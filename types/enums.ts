export const USER_ROLES = ["admin", "reception", "user"] as const;

export const STATUS = ["active", "inactive"] as const;

export const GENDER = ["ذكر", "أنثى"] as const;

export const APPOINTMENT_STATUS = [
    "pending",
    "approved",
    "rejected",
    "cancelled",
] as const;